services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - auth_data:/data
    environment:
      - SECRET_KEY=${SECRET_KEY:-change-me-in-production}
      - DATABASE=/data/secure_auth.db
    restart: unless-stopped

volumes:
  auth_data:
